package com.pluralsight;

public class Dice {

//Create a method named "roll" to generate random numbers between 1 & 6

    public int roll(){
        int randomNumber = (int) (Math.random() * 6) + 1;
        return randomNumber;
    }










}
